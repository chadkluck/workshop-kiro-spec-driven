# Prompts for Examples

## Convert parameters.json format

```text
Convert parameters.json from an array of `ParameterKey`/`ParameterValue` to object property keys where {ParameterKey1: ParameterValue1, etc} and save as parameters-new.json
```

## Summarize README-EXAMPLE.md

```text
Give me the key points of README-EXAMPLE.md and why I would use the project
```

## Convert to README-EXAMPLE.md to Word

```text
Convert README-EXAMPLE.md to a Word document ensuring the links still work.
```