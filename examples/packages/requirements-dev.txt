# For local development, install requirements.txt first, then requirements-dev.txt

# For local development, install using a Python virtual environment
# python3 -m venv .ve
# source .ve/bin/activate
# pip install -r requirements.txt
# pip install -r requirements-dev.txt

# AWS SDK - already available in Lambda runtime, but included for local testing
boto3>=1.42.0
botocore>=1.42.0

# moto[all]==5.0.18  # This package is 500MB+ and can crash the system
# Only install if absolutely necessary and monitor system resources

# Alternative: Use moto without [all] extras for specific services only
moto[sqs,dynamodb,lambda,cloudfront,s3,events,iam,logs,sns,sts]==5.0.18  # Smaller, service-specific installation